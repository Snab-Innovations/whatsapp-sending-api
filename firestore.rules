rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // --- Session Credentials & Passcode Collection ---
    // Stores Session IDs, Passcodes, and WhatsApp Connection Status
    match /sessions/{sessionId} {
      allow read, write: if true;
    }
  }
}

